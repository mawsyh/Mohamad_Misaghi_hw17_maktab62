"use strict";

var resolveException = require("type/lib/resolve-exception")
  , is               = require("type/value/is");

module.exports = function (value/*, options*/) {
	if (is(value)) return value;
	return resolveException(value, "Cannot use %v", arguments[1]);
};
